diff -ur ./etc_orig/asterisk/extensions.conf ./etc/asterisk/extensions.conf
--- ./etc_orig/asterisk/extensions.conf
+++ ./etc/asterisk/extensions.conf
@@ -855,3 +855,8 @@
 ; "core show functions" will list all dialplan functions
 ; "core show function <COMMAND>" will show you more information about
 ; one function. Remember that function names are UPPER CASE.
+
+[outgoing]
+exten => _X.,1,Dial(SIP/${EXTEN})
+exten => _X.,n,Answer()
+;exten => _X.,n,Hangup()
diff -ur ./etc_orig/asterisk/http.conf ./etc/asterisk/http.conf
--- ./etc_orig/asterisk/http.conf
+++ ./etc/asterisk/http.conf
@@ -26,17 +26,17 @@
 ; Whether HTTP/HTTPS interface is enabled or not.  Default is no.
 ; This also affects manager/rawman/mxml access (see manager.conf)
 ;
-;enabled=yes
+enabled=yes
 ;
 ; Address to bind to, both for HTTP and HTTPS. You MUST specify
 ; a bindaddr in order for the HTTP server to run. There is no
 ; default value.
 ;
-bindaddr=127.0.0.1
+bindaddr=0.0.0.0
 ;
 ; Port to bind to for HTTP sessions (default is 8088)
 ;
-;bindport=8088
+bindport=8088
 ;
 ; Prefix allows you to specify a prefix for all requests
 ; to the server.  The default is blank.  If uncommented
Only in ./etc/asterisk: keys
diff -ur ./etc_orig/asterisk/rtp.conf ./etc/asterisk/rtp.conf
--- ./etc_orig/asterisk/rtp.conf
+++ ./etc/asterisk/rtp.conf
@@ -34,7 +34,7 @@
 ; probation=8
 ;
 ; Whether to enable or disable ICE support. This option is disabled by default.
-; icesupport=true
+icesupport=true
 ;
 ; Hostname or address for the STUN server used when determining the external
 ; IP address and port an RTP session can be reached at. The port number is
@@ -43,7 +43,7 @@
 ;
 ; e.g. stundaddr=mystun.server.com:3478
 ;
-; stunaddr=
+stunaddr=stun.l.google.com:19302
 ;
 ; Hostname or address for the TURN server to be used as a relay. The port
 ; number is optional. If omitted the default value of 3478 will be used.
diff -ur ./etc_orig/asterisk/sip.conf ./etc/asterisk/sip.conf
--- ./etc_orig/asterisk/sip.conf
+++ ./etc/asterisk/sip.conf
@@ -132,7 +132,7 @@
 ;allowtransfer=no               ; Disable all transfers (unless enabled in peers or users)
                                 ; Default is enabled. The Dial() options 't' and 'T' are not
                                 ; related as to whether SIP transfers are allowed or not.
-;realm=mydomain.tld             ; Realm for digest authentication
+realm=172.25.10.69              ; Realm for digest authentication
                                 ; defaults to "asterisk". If you set a system name in
                                 ; asterisk.conf, it defaults to that system name
                                 ; Realms MUST be globally unique according to RFC 3261
@@ -177,7 +177,7 @@
 ; depends on the operating system. On systems using glibc, AAAA records are given
 ; priority.
 
-udpbindaddr=0.0.0.0             ; IP address to bind UDP listen socket to (0.0.0.0 binds to all)
+udpbindaddr=0.0.0.0:5060            ; IP address to bind UDP listen socket to (0.0.0.0 binds to all)
                                 ; Optionally add a port number, 192.168.1.1:5062 (default is port 5060)
 
 ; When a dialog is started with another SIP endpoint, the other endpoint
@@ -235,7 +235,7 @@
                                 ; receiving clients are slow to process the received information.
                                 ; Value is in milliseconds; default is 100 ms.
 
-transport=udp                   ; Set the default transports.  The order determines the primary default transport.
+transport=udp,ws                ; Set the default transports.  The order determines the primary default transport.
                                 ; If tcpenable=no and the transport set is tcp, we will fallback to UDP.
 
 srvlookup=yes                   ; Enable DNS SRV lookups on outbound calls
@@ -1517,7 +1517,7 @@
 ;insecure=port                   ; Allow matching of peer by IP address without
                                  ; matching port number
 ;insecure=invite                 ; Do not require authentication of incoming INVITEs
-;insecure=port,invite            ; (both)
+insecure=port,invite            ; (both)
 ;qualify=1000                    ; Consider it down if it's 1 second to reply
                                  ; Helps with NAT session
                                  ; qualify=yes uses default value
@@ -1571,3 +1571,57 @@
                                 ; if the nat option is enabled. If a single RTP packet is received Asterisk will know the
                                 ; external IP address of the remote device. If port forwarding is done at the client side
                                 ; then UDPTL will flow to the remote device.
+
+
+
+;extension to use on web client
+[6001]
+host=dynamic
+secret=6001
+context=outgoing
+type=peer
+encryption=yes
+avpf=yes
+icesupport=yes
+transport=ws,wss,udp
+directmedia=no
+disallow=all
+allow=all
+dtlsenable=yes
+dtlsverify=fingerprint
+dtlscertfile=/etc/asterisk/keys/asterisk.pem
+dtlscafile=/etc/asterisk/keys/ca.crt
+dtlssetup=actpass
+insecure=port,invite
+
+;extension to use on web client
+[6000]
+host=dynamic
+secret=6000
+context=outgoing
+type=peer
+encryption=yes
+avpf=yes
+icesupport=yes
+transport=ws,wss,udp
+directmedia=no
+disallow=all
+allow=all
+dtlsenable=yes
+dtlsverify=fingerprint
+dtlscertfile=/etc/asterisk/keys/asterisk.pem
+dtlscafile=/etc/asterisk/keys/ca.crt
+dtlssetup=actpass
+insecure=port,invite
+
+;extension to use on softphones such as twinkle, linphone,ekiga..etc
+[6002]
+host=dynamic
+secret=6002
+context=outgoing
+type=peer
+transport=ws,wss,udp
+directmedia=no
+disallow=all
+allow=all
+insecure=port,invite
diff -ur ./etc_orig/httpd/conf/httpd.conf ./etc/httpd/conf/httpd.conf
--- ./etc_orig/httpd/conf/httpd.conf
+++ ./etc/httpd/conf/httpd.conf
@@ -73,7 +73,7 @@
 # KeepAlive: Whether or not to allow persistent connections (more than
 # one request per connection). Set to "Off" to deactivate.
 #
-KeepAlive Off
+KeepAlive On
 
 #
 # MaxKeepAliveRequests: The maximum number of requests to allow
@@ -133,7 +133,7 @@
 # prevent Apache from glomming onto all bound IP addresses (0.0.0.0)
 #
 #Listen 12.34.56.78:80
-Listen 80
+Listen 0.0.0.0:80
 
 #
 # Dynamic Shared Object (DSO) Support
@@ -273,7 +273,7 @@
 # You will have to access it by its address anyway, and this will make 
 # redirections work in a sensible way.
 #
-#ServerName www.example.com:80
+ServerName 172.25.10.69:80
 
 #
 # UseCanonicalName: Determines how Apache constructs self-referencing 
@@ -335,7 +335,7 @@
 # It can be "All", "None", or any combination of the keywords:
 #   Options FileInfo AuthConfig Limit
 #
-    AllowOverride None
+    AllowOverride AuthConfig FileInfo
 
 #
 # Controls who can get stuff from this server.
@@ -399,7 +399,7 @@
 # negotiated documents.  The MultiViews Option can be used for the 
 # same purpose, but it is much slower.
 #
-DirectoryIndex index.html index.html.var
+DirectoryIndex index.html index.htm index.html.var
 
 #
 # AccessFileName: The name of the file to look for in each directory
@@ -533,7 +533,7 @@
 # Set to "EMail" to also include a mailto: link to the ServerAdmin.
 # Set to one of:  On | Off | EMail
 #
-ServerSignature On
+ServerSignature Off
 
 #
 # Aliases: Add here as many aliases as you need (with no limit). The format is 
@@ -756,7 +756,7 @@
 # in HTML content to override this choice, comment out this
 # directive:
 #
-AddDefaultCharset UTF-8
+# AddDefaultCharset UTF-8
 
 #
 # AddType allows you to add to or override the MIME configuration
Only in ./etc/httpd/conf.d: ssl.conf
Only in ./etc_orig/httpd/conf.d: welcome.conf
Only in ./etc/httpd/logs: access_log
Only in ./etc/httpd/logs: access_log-20150614
Only in ./etc/httpd/logs: error_log
Only in ./etc/httpd/logs: error_log-20150614
Only in ./etc/httpd/logs: ssl_access_log
Only in ./etc/httpd/logs: ssl_access_log-20150614
Only in ./etc/httpd/logs: ssl_error_log
Only in ./etc/httpd/logs: ssl_request_log
Only in ./etc/httpd/logs: ssl_request_log-20150614
Only in ./etc/httpd/modules: mod_ssl.so
Only in ./etc/httpd/run: httpd.pid
diff -ur ./etc_orig/sysconfig/grub ./etc/sysconfig/grub
--- ./etc_orig/sysconfig/grub
+++ ./etc/sysconfig/grub
@@ -1,2 +1,2 @@
-boot=/dev/sda
+boot=/dev/sde
 forcelba=0
diff -ur ./etc_orig/sysconfig/iptables ./etc/sysconfig/iptables
--- ./etc_orig/sysconfig/iptables
+++ ./etc/sysconfig/iptables
@@ -1,13 +1,14 @@
-# Firewall configuration written by system-config-firewall
-# Manual customization of this file is not recommended.
+# Generated by iptables-save v1.4.7 on Wed Jun 10 20:15:15 2015
 *filter
 :INPUT ACCEPT [0:0]
 :FORWARD ACCEPT [0:0]
-:OUTPUT ACCEPT [0:0]
--A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
--A INPUT -p icmp -j ACCEPT
--A INPUT -i lo -j ACCEPT
--A INPUT -m state --state NEW -m tcp -p tcp --dport 22 -j ACCEPT
--A INPUT -j REJECT --reject-with icmp-host-prohibited
--A FORWARD -j REJECT --reject-with icmp-host-prohibited
+:OUTPUT ACCEPT [17:17444]
+-A INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT 
+-A INPUT -p icmp -j ACCEPT 
+-A INPUT -i lo -j ACCEPT 
+-A INPUT -p tcp -m state --state NEW -m tcp --dport 22 -j ACCEPT 
+-A INPUT -p tcp -m tcp --dport 80 -j ACCEPT 
+-A INPUT -j REJECT --reject-with icmp-host-prohibited 
+-A FORWARD -j REJECT --reject-with icmp-host-prohibited 
 COMMIT
+# Completed on Wed Jun 10 20:15:15 2015
Only in ./etc/sysconfig: iptables.save
diff -ur ./etc_orig/sysconfig/network ./etc/sysconfig/network
--- ./etc_orig/sysconfig/network
+++ ./etc/sysconfig/network
@@ -1,2 +1,2 @@
 NETWORKING=yes
-HOSTNAME=asterisk-dev-vm.localdomain
+HOSTNAME=asterisk-dev.lohika
diff -ur ./etc_orig/sysconfig/network-scripts/ifcfg-eth0 ./etc/sysconfig/network-scripts/ifcfg-eth0
--- ./etc_orig/sysconfig/network-scripts/ifcfg-eth0
+++ ./etc/sysconfig/network-scripts/ifcfg-eth0
@@ -1,7 +1,7 @@
 DEVICE=eth0
-HWADDR=08:00:27:64:E7:D4
+HWADDR=E0:69:95:D1:62:AB
 TYPE=Ethernet
-UUID=70341f0d-b6e9-4247-811d-ed49ac021bce
+UUID=b67800ca-a303-4411-82a1-e3f05f56e0c6
 ONBOOT=yes
 NM_CONTROLLED=yes
 BOOTPROTO=dhcp
Only in ./etc_orig/sysconfig/network-scripts: ifcfg-eth1
diff -ur ./etc_orig/sysconfig/selinux ./etc/sysconfig/selinux
--- ./etc_orig/sysconfig/selinux
+++ ./etc/sysconfig/selinux
@@ -4,7 +4,7 @@
 #     enforcing - SELinux security policy is enforced.
 #     permissive - SELinux prints warnings instead of enforcing.
 #     disabled - No SELinux policy is loaded.
-SELINUX=enforcing
+SELINUX=disabled
 # SELINUXTYPE= can take one of these two values:
 #     targeted - Targeted processes are protected,
 #     mls - Multi Level Security protection.
